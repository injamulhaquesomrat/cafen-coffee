const SectionTitle = ({ title }) => {
  return (
    <h2 className="text-cf-coffee text-[36px] sm:text-[40px] md:text-[42px] lg:text-5xl xl:text-[55px] font-sober">
      {title}
    </h2>
  );
};

export default SectionTitle;
